machine:
  environment:
    TERM: dumb
  java:
    version: openjdk7
    version: openjdk8

test:
  override:
    - gradlew -d clean check jacocoTestReport
  post:
    - cp -R build/reports/* $CIRCLE_ARTIFACTS
